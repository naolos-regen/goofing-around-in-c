# define IS_NAN(x)	is_nan(x)
# define ABS(x)		((x) * (((x) < 0 ) * -1 + ((x) >= 0) * 1))
# define D_MAX_VAL	1.797693e+308
# define INF		
# define D_MIN_VAL	2.225074e-308
# define SCALB(z, n)	

// nah i give up xd
